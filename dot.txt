# creating conda environment and activating it
conda create --prefix ./env python=3.12
conda activate ./env
conda deactivate ./env
which python
conda env export > environment.yml
conda env export --from-history > environment.yml
conda env export --from-history | grep -v "^prefix: " > enviornment.yml

# creating python environment and activating it
python3 -m venv env
source env/bin/activate
pip install --upgrade pip
which python
deactivate 
pip freeze > requirements.txt
pip list --not-required --format=freeze > requirements.txt

# getting all the user installed libraries and version
pip list --not-required
